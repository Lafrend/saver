"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = useMode;
var _antd = require("antd");
function useMode(mode = 'auto') {
  const {
    md
  } = _antd.Grid.useBreakpoint();
  const isSimple = mode === 'simple' || mode === 'auto' && !md;
  return {
    isSimple,
    md
  };
}