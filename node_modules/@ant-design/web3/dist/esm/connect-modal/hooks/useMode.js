import { Grid } from 'antd';
export default function useMode() {
  var mode = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'auto';
  var _Grid$useBreakpoint = Grid.useBreakpoint(),
    md = _Grid$useBreakpoint.md;
  var isSimple = mode === 'simple' || mode === 'auto' && !md;
  return {
    isSimple: isSimple,
    md: md
  };
}