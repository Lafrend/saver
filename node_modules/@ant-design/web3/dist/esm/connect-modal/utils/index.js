import { TinyColor } from '@ctrl/tinycolor';
export var mergeReactNodeProps = function mergeReactNodeProps(node, defaultNode, resolveString) {
  if (node === null || node === false) return node;
  if (typeof node === 'string' && resolveString) return resolveString(node);
  return node !== null && node !== void 0 ? node : defaultNode;
};
export var defaultGroupOrder = function defaultGroupOrder(a, b) {
  if (a === 'Popular' && b !== 'Popular') {
    return -1;
  }
  if (a === 'More' && b !== 'More') {
    return 1;
  }
  return a.localeCompare(b);
};
export var isDarkTheme = function isDarkTheme(token) {
  var hsv = new TinyColor(token.colorFill).onBackground(token.colorBgElevated).toHsv();
  return hsv.v < 0.5;
};