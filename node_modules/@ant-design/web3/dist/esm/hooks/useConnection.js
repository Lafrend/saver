import useProvider from "./useProvider";
export default function useConnection() {
  var _useProvider = useProvider(),
    connect = _useProvider.connect,
    disconnect = _useProvider.disconnect;
  return {
    connect: connect,
    disconnect: disconnect
  };
}